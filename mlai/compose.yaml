networks:
  networking:
    external: true
  management:
    external: true
  monitoring:
    external: true
  security:
    external: true
  tools:
    external: true
  databases:
    external: true
  storage:
    external: true
  mediaserver:
    external: true

include:
  # Local AI
  #- local-ai.yaml
  # Open WebUI
  - open-webui.yaml
